/* eslint-disable arrow-body-style */
const sinon = require('sinon');
// const { expect } = require('chai');

describe('Test angularGenerator', () => {
  let sandbox;

  beforeEach(async () => {
    sandbox = sinon.createSandbox();
  });

  afterEach(() => sandbox.restore());

  it('should unit test ', () => {
  });
});
